package com.cypher.assistant.features.youtube.domain.model

/**
 * Recognized visual screens / contexts inside the YouTube application.
 */
enum class YouTubeScreenType {
    UNKNOWN,
    HOME,
    SEARCH_INPUT,
    SEARCH_RESULTS,
    VIDEO,
    SHORTS,
    COMMENTS,
    CHANNEL,
    SUBSCRIPTIONS,
    HISTORY,
    NOTIFICATIONS
}
